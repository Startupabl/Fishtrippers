Plan:
1. Update the admin settings route so the settings grid only shows Categories and Page Settings.
2. Leave the existing Platform Communications route/page in place so it can still be accessed directly if needed.
3. Remove the now-unused Mail icon import from the settings route.
4. Verify the settings page no longer renders the Platform Communications card.