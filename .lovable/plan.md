Remove the second line directly under the "DUE NOW TO BOOK" header in the expanded trip card payment summary. In `src/components/operator-listing/TripsBlock.tsx`, delete the `<div className="mt-0.5 text-xs text-emerald-900/70">Charged today to secure your spot</div>` element while keeping the "Due Now to Book" header and the deposit amount.

No data model, pricing, or dependency changes. Pure UI copy removal.