## Change

In `src/components/operator-onboarding/trips/TripFormDialog.tsx` (line 323), change the button label inside the "What you'll target" section from:

- `Edit capabilities` → `Edit Target Fish Options`

No other changes.