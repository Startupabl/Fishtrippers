In `src/routes/_authenticated/settings.profile.tsx`:

- Display name `placeholder`: `"e.g. Michael C."` → `"e.g. Christopher C."`
- Motto `placeholder`: `"e.g. Stay curious, build often."` → `"e.g. Tight lines, calm waters."`

No other changes.